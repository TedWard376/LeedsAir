ktor:
    application:
        modules:
            - flightbooking.ApplicationKt.module
    deployment:
        port: 8080

database:
    jdbcUrl: ${?DB_JDBC_URL}
    username: ${?DB_USER}
    password: ${?DB_PASSWORD}
    driverClassName: org.postgresql.Driver
    maximumPoolSize: 10
